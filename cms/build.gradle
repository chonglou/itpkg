apply plugin: 'java'
apply plugin: 'idea'

repositories {
    mavenCentral()
    maven { url "http://repository.pentaho.org/artifactory/repo/" }
}

dependencies {
    compile project(':core')

    testCompile('junit:junit')
}

jar {
    baseName = appName+"-cms"
}
